import SurveyDesigner from "@/components/surveys/SurveyDesigner";

export const metadata = {
    title: "Survey Designer | Project Mgmt",
    description: "Design your surveys with a familiar interface",
};

export default function SurveyDesignerPage() {
    return <SurveyDesigner />;
}
